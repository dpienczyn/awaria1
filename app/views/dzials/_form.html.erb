<%= form_for(dzial) do |f| %>
  <% if dzial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dzial.errors.count, "error") %> prohibited this dzial from being saved:</h2>

      <ul>
      <% dzial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nazwa %>
    <%= f.text_area :nazwa %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
