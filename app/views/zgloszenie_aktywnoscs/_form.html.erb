<%= form_for(zgloszenie_aktywnosc) do |f| %>
  <% if zgloszenie_aktywnosc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zgloszenie_aktywnosc.errors.count, "error") %> prohibited this zgloszenie_aktywnosc from being saved:</h2>

      <ul>
      <% zgloszenie_aktywnosc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="jumbotron">
<div class="field">
  <%= f.label :Zgłoszenie %><br/>
  <%= f.text_field :zgloszenie_id, class:"form-control" %>
  </div><br/>

  <div class="field">
    <%= f.label :Treść %><br/>
    <%= f.text_area :tresc, class:"form-control" %>
  </div><br/>
</div>

<div class="jumbotron">
  <div class="actions">
            <%= f.submit "Dodaj", class:'btn btn-sm btn-success btn-create' %>
            <%= link_to 'Pokaż', @zgloszenie_aktywnosc, class: "btn btn-sm btn-primary btn-create" %>
            <%= link_to 'Wróć', zgloszenie_aktywnoscs_path, class: "btn btn-sm btn-danger btn-create" %>
            </div>
<% end %><br />
</div>
