<%= form_for(zgloszenie_aktywnosc) do |f| %>
  <% if zgloszenie_aktywnosc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zgloszenie_aktywnosc.errors.count, "error") %> prohibited this zgloszenie_aktywnosc from being saved:</h2>

      <ul>
      <% zgloszenie_aktywnosc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= f.label :Zgłoszenie %><br/>
  <%= f.text_field :zgloszenie_id%>
  </div><br/>

  <div class="field">
    <%= f.label :Treść %><br/>
    <%= f.text_area :tresc%>
  </div><br/>

  <div class="actions">
            <%= f.submit "Dodaj", class:'btn btn-success' %>
            <%= link_to 'Pokaż', @zgloszenie_aktywnosc, class: "btn btn-primary" %>
            <%= link_to 'Wróć', zgloszenie_aktywnoscs_path, class: "btn btn-danger" %>
            </div>
<% end %><br />