<% content_for :title, " - Zaloguj się!!" %>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      <fieldset>
        <h2 class="text-center">Zaloguj się</h2>
        <hr class="colorgraph">
        <div class="form-group">
          <%= f.email_field :email, autofocus: true, class: "form-control input-lg", placeholder: "Adres email" %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, autocomplete: "off", class: "form-control input-lg", placeholder: "Hasło" %>
        </div>
        <span class="button-checkbox">
          <button type="button" class="btn" data-color="info">Zapamietaj mnie</button>
          <%= f.check_box :remember_me, class: "hidden" if devise_mapping.rememberable? %>
          <%= link_to "Zapomniałeś hasła?", new_password_path(resource_name), class: "btn btn-link pull-right" %>
        </span>
        <hr class="colorgraph">
        <div class="row">
          <div class="form-group">
            <div class="col-xs-12 col-md-6">
              <%= f.submit "Zaloguj się", class: "btn btn-success btn-block btn-lg" %>
            </div>
            <div class="col-xs-12 col-md-6">
              <%= link_to "Rejestracja", new_registration_path(resource_name), class: "btn btn-primary btn-block btn-lg"  %>
            </div>
          </div>
        </div>
      </fieldset>
    </div>
  </div>
</div>
<% end %>

