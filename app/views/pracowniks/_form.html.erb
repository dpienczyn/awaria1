<%= form_for(pracownik) do |f| %>
  <% if pracownik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pracownik.errors.count, "error") %> prohibited this pracownik from being saved:</h2>

      <ul>
      <% pracownik.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= f.label :stanowisko_id %>
    <%= f.text_field :stanowisko_id %>
  </div> -->

   <div class="field">
     <%= f.label :stanowisko %><br />
     <%= f.collection_select :stanowisko_id, Stanowisko.all, :id, :nazwa, prompt: "Wybierz" %>
   </div><br/>

  <div class="field">
    <%= f.label :dzial_id %><br />
    <%= f.collection_select :dzial_id, Dzial.all, :id, :nazwa, prompt: "Wybierz" %>
  </div><br />

  <div class="field">
    <%= f.label :nazwa %><br />
    <%= f.text_area :nazwa %>
  </div><br/>

  <!-- <div class="field">
    <%= f.label :stanowisko %>
    <%= f.text_area :stanowisko %>
  </div> -->

  <div class="field">
    <%= f.label :telefon %><br />
    <%= f.number_field :telefon %>
  </div><br/>

  <div class="field">
    <%= f.label :email %><br/>
    <%= f.text_area :email %>
  </div><br />

  <div class="actions">
    <%= f.submit :Aktualizuj, class: "btn btn-success" %>
  </div>
<% end %><br />
